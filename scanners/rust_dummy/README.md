
# Rust Dummy Scanner

A dummy scanner to demonstrate how an Elos scanner can be written in Rust.

It Publishes a new event every second cycling through severity levels.

## Building

To build this scanner you can use cmake. That way the scanner gets named in same naming convention as other scanner plugins.
But running cargo directly also works.

